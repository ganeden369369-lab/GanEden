{
  "name": "gan-eden",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "db:types": "npx -y supabase@latest gen types typescript --local > packages/shared/src/database.types.ts",
    "functions:vendor": "node scripts/vendor-functions.mjs",
    "functions:serve": "node scripts/vendor-functions.mjs && npx -y supabase@latest functions serve --env-file supabase/functions/.env.local"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0"
  }
}
